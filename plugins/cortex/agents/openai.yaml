interface:
  display_name: "Cortex"
  short_description: "Durable multi-agent coordination"

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "cortex"
      description: "Cortex durable coordination MCP server"
