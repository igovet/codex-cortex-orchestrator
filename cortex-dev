#!/usr/bin/env bash
# Convenience entry point for the one isolated development launcher.
set -euo pipefail
script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
exec "${script_dir}/scripts/cortex-dev" "$@"
