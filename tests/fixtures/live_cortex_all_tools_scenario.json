{
  "prompt_file": "tests/fixtures/live_cortex_all_tools_prompt.txt",
  "model": "gpt-5.6-luna",
  "reasoning_effort": "high",
  "user_turns": [
    "Показывай сначала невыполненные записи, затем выполненные, внутри групп — от новых к старым.",
    "Одобряю показанный план.",
    "Уточняю только обработку повреждённого файла: сохрани исходный файл рядом с резервным суффиксом, создай чистое хранилище и покажи понятное предупреждение. Остальной объём не меняй.",
    "Подтверждаю это точечное изменение.",
    "Продолжай ту же задачу после восстановления с места, подтверждённого текущим состоянием.",
    "Закрывай задачу после показа хронологии, плана и итоговых отчётов."
  ],
  "operator_actions": [
    "Submit the initial prompt only after the real composer is visible.",
    "Answer the product clarification only after the durable question is visibly presented.",
    "Approve only after the decision-ready plan summary and verified plan link are visible.",
    "Submit the point refinement after review so retirement uses the exact current outcome and reads only that outcome.",
    "After a delivery assignment is visibly dispatched but before its publication, stop only the named CLI smoke session and resume the same ordinary thread with --resume-last; do not create a new task.",
    "Before closure, require the visible newest-first chronology and verified plan/report links.",
    "Run the unchanged stamped candidate in CLI and Desktop consecutively, in either order."
  ],
  "required_operations": [
    "open_task",
    "read_task",
    "read_state",
    "read_scope",
    "read_outcome",
    "read_continuations",
    "read_evidence",
    "read_timeline",
    "open_clarification",
    "record_clarification",
    "open_plan_review",
    "record_plan_review",
    "open_steering",
    "record_steering",
    "open_assignment",
    "publish_plan",
    "publish_result",
    "publish_documentation",
    "assess_governance",
    "close_task"
  ],
  "acceptance": [
    "Every required operation has at least one successful task-relevant event on each host.",
    "No Cortex tool error, validation error, schema error, traceback, hidden worker error, or unexplained successful mutation replay occurs.",
    "The resumed CLI continuation retains the existing task and does not call open_task again.",
    "After resume, one scalar state read routes unfinished delegated work to read_continuations; read_timeline is not used as a continuation lookup.",
    "Repeated bounded waits while a worker remains active do not trigger read_state polling.",
    "The coordinator never receives worker-only authority and every worker sees only its immutable selected scope.",
    "The user sees decision context, the verified plan link, verified report links, and the requested chronology before closure.",
    "CLI and Desktop pass consecutively on the same unchanged cache-stamped payload."
  ]
}
