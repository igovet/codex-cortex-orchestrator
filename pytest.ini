[pytest]
# Task-tree tests are executed by the Phase 2 fixture oracle from each
# disposable worktree. They are not repository-level tests: collecting them
# here would run outside their intended source-root import context.
norecursedirs = .git .tox dist build *.egg* tests/fixtures/phase2_cli_v1/trees
