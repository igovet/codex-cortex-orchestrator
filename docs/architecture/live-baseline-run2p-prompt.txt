$cortex:orchestrator

Create a polished, accessible, self-contained single-page HTML product landing page in this empty project. The page must work by opening index.html directly, use only embedded HTML, CSS, and JavaScript, and include a responsive header, hero, exactly three feature cards, an interactive theme toggle, a compact FAQ, and a footer. Use semantic HTML, visible keyboard focus, reduced-motion support, readable contrast, and no external assets or network dependencies. Before implementation, ask me one product question about the accent color, then show me the plan and wait for my approval. After implementation, independently verify the page structure, interactions, keyboard behavior, responsive layout, and offline operation. Add a small README explaining how to open and verify the page.
