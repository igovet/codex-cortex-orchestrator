[$cortex:orchestrator] This is already an isolated live-dev ordinary Codex session. Do not start nested tmux, cortex-dev, codex exec, shell-based coordinator validation, or coordinator repository inspection.

Create a polished, accessible, self-contained single-page HTML product landing page in this empty test project. The page must work by opening index.html directly, use only embedded HTML/CSS/JavaScript, and include a responsive header, hero, three feature cards, an interactive theme toggle, a compact FAQ interaction, and a footer. Use semantic HTML, visible keyboard focus, reduced-motion support, readable contrast, and no external assets or network dependencies. Add a small project README explaining how to open and verify the page.

Exercise the complete Cortex orchestration lifecycle. Before planning, ask exactly one genuine product clarification about the accent color and wait for the answer. Then use a planner worker to produce a durable plan, visibly present that plan for explicit approval, and wait. After approval, continue through implementation, independent verification, documentation-impact assessment or documentation work as evidence requires, and advisory closure. Every worker must publish its own complete durable evidence on its first valid publication attempt. Do not repeat a successful mutation unless an earlier transport result was explicitly ambiguous. Treat every tool error, validation error, hidden worker error, duplicate logical mutation, or unexplained replay as a failure and report it honestly instead of masking it.

Acceptance requires that the final page and README exist in this test project; implementation verification checks the page structure and interactions without network access; an independent worker verifies accessibility-relevant structure, keyboard behavior, responsive behavior, and the stated acceptance requirements; downstream workers consume only declared durable predecessor evidence; and the final coordinator response identifies decisive verification evidence and any residual risk.
