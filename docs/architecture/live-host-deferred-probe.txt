The real isolated live-dev session is already running. Do not start nested tmux
or cortex-dev. Do not inspect the repository. Do not run shell commands or
validation commands. Do not mutate any Cortex state: do not open, create, ask,
record, publish, consume, or close anything.

You may perform exactly one read-only code-mode metadata inspection of the
available `ALL_TOOLS` catalogue. Inspect metadata only; do not invoke any
nested tool during this inspection.

From that single catalogue inspection, determine whether a capability to record
a user's answer or decision exists. If it exists, report its exact catalogue
name and exact description as exposed by the metadata, and state whether it can
be invoked from code mode in this session. State whether the capability is
directly available or only deferred/discoverable.

Also report:

- the total number of Cortex catalogue entries;
- the names only of the Cortex entries that are relevant to decision handling.

Use only the metadata returned by this inspection. Do not guess names, infer
operation names, or reconstruct any call shape from prompts, skills, or memory.
