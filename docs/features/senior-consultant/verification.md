# Senior consultant and automatic provider verification — 2026-09-08

## Final candidate live qualification

Candidate `1.15.8+codex.sha256.3443aff2cf72630c` completed the focused CLI scenario
with **audit exit zero**: all 50 host call records and 13 MCP outcomes were
reviewed. There were no errors, truncations, replayed writes, policy violations
or open execution resources. One Sol medium consultant read two selected reports,
patched its own issued draft once and published one advisory. The coordinator
published two pipeline editions and retained acceptance. The source hash cited
by the consultant matches the archived request. CLI exited zero and was stopped
before Desktop preparation on the unchanged payload.

The consultation used 10 Sol responses: 254,555 input tokens (206,080 cached),
2,047 output, 256,602 total. Coordinator plus consultant used 790,954 total tokens
in a 264.343-second coordinator turn.

The immediately following actual Desktop run passed **audit exit zero** on the
same unchanged payload and dependency identity. All 62 host call records and
14 MCP outcomes were reviewed; every server operation succeeded, with no errors,
truncations, replayed writes, policy violations or open execution resources.
The consultant read the one selected source, patched its issued draft once and
published once. Its cited source hash matches the archived file. The coordinator
published two pipeline editions and retained acceptance. The prepared and final
UI were inspected; the send helper observed exactly one new task receipt.
An X11 desktop-property query emitted a warning, but the send command exited zero
and its receipt plus the completed UI independently established submission.
The disposable Desktop session was stopped after final review.

Desktop used 10 Sol responses: 325,757 input tokens (287,232 cached), 1,697 output,
327,454 total. All participants used 1,255,822 tokens in a 263.605-second
coordinator turn. Across this clean pair, two consultations used 20 Sol responses,
87,000 non-cached input tokens and 3,744 output tokens; totals are token receipts,
not a monetary invoice.

This qualifies the focused default Sol medium consultation route in consecutive
CLI then Desktop runs. It does not certify encrypted assignment content or the
unrun scenarios listed under Limits. The observed default route is established
from the registered native task name, explicit model/effort and empty inherited
context; the observer still rejects other opaque routes.

The earlier rejected runs below remain recorded; they are not passing evidence.

## Follow-up repair

The final instruction candidate is `1.15.8+codex.sha256.3443aff2cf72630c`.
Draft discovery and creation now explicitly require copying patch context from
the returned Markdown instead of reconstructing placeholder names. Package and
source-only sync validation passed, followed by 382 tests in 13.77 seconds.
It also supplies exact relative links to both consultant instruction files,
prohibiting directory discovery while preparing the assignment.

The preceding `9baa2fac744ec5d1` candidate completed CLI with 60 reviewed calls,
13 successful MCP outcomes and audit exit zero. One consultation used 11 Sol
responses: 282,403 input (256,512 cached), 2,460 output, 284,863 total. All
participants used 964,796 tokens in 294.753 seconds. Its subsequent Desktop run
was rejected because the coordinator used `find` to discover the publication
reference. All 25 current calls and six MCP outcomes were reviewed and a failing
audit captured before stopping. No tool execution error was found; the access
violation remains a rejection. It does not qualify consecutive parity.

Candidate `9baa2fac744ec5d1`
It repeats the individual-discovery rule at the very beginning of the native
assignment, before the full attached instruction bodies. Package validation,
source-only sync and 382 tests passed sequentially (13.70 seconds, eight warnings).

Candidate `1.15.8+codex.sha256.96a793f34465353a` makes missing-tool discovery
individual and bounded, requires a syntactically complete publication call using
only live declared fields, and prohibits inaccurate copied hashes or unsupported
comparison claims. The coordinator uses the registered native consultant name.
The observer can verify the default Sol medium route from that exact name,
explicit model/effort and no inherited conversation; the encrypted assignment
body remains unavailable. The new negative cases retain an unverified result for
other opaque names, inherited contexts, non-default effort and Astra routes, and
still reject disallowed models. No forbidden-access or failed-tool check was removed.

The 43 focused tests passed, followed by package validation, source-only sync and
all **382 tests passing in 13.82 seconds**, with eight aiohttp request-key warnings.
The previous rejected observations below remain evidence for the earlier candidate.

The focused CLI repair scenario completed one consultation and passed its final
audit (exit zero): all 72 call records and 17 MCP outcomes were reviewed, with no
errors, truncation, replayed writes, access violations or open execution resources.
The consultant read the selected source, edited its issued draft and published
once. The coordinator retained acceptance. The CLI exited zero and was stopped
before Desktop preparation on the unchanged candidate. This consultation used
12 Sol medium responses: 301,739 input tokens (265,088 cached), 1,983 output,
303,722 total; coordinator plus consultant used 1,438,086 total tokens in a
318.672-second coordinator turn.

The following Desktop trial on `96a793f34465353a` was rejected. It added a Luna
explorer with inherited conversation and opaque assignment, then the consultant
again dumped multiple descriptions and received truncated output. All 82 current
call records and 20 MCP outcomes were inspected before a failing audit and stop;
the failures were not waived. The interrupted consultant had three recorded Sol
responses (106,773 input, 66,944 cached, 464 output). That trial is not parity
qualification. The final focused workload explicitly requests the consultation
alone from already supplied evidence, without preliminary investigation.


The first CLI run on `ef866f2d22c0e548` was rejected after an otherwise compliant
consultation: the coordinator patched a nonexistent placeholder in its second
pipeline draft, then corrected the patch and completed. All 52 calls and 12 MCP
outcomes were reviewed; audit exit one retained the recovered host error and no
access violations. The consultant used nine Sol responses: 224,251 input
(205,696 cached), 1,715 output, 225,966 total. All participants used 1,002,828
tokens in 253.538 seconds. Source checks for this candidate passed 382 tests in
13.84 seconds after package and source-only synchronization validation.

The Desktop run started after that rejected CLI was stopped at the user’s
request before product work: nine observed calls, three initialization events,
no errors yet, and no completed consultation. It is not qualification. Desktop
will be run only after a clean CLI result.

## Previous candidate

The previous candidate was `1.15.8+codex.sha256.0ba3422303f67543`
(semantic version 1.15.8 was explicitly retained). It preserves the seven MCP
operations and all 22 executor profiles, adding the separate consultant profile.
Runtime changes are confined to the plugin. Stable installation and configuration
were not updated. The complete illustrative cycle is in the [feature guide](index.md).

## Source checks

Package validation, source-only synchronization checks and the full suite ran
sequentially: **381 tests passed, with eight warnings, in 13.43 seconds** on
the final recheck. This run used the launcher's verified dependency directory
on `PYTHONPATH`; plain system Python first passed 379 tests and skipped the two
Zstandard cases because that module was unavailable. The dependency-backed rerun
had no skips. The eight warnings concern aiohttp request-key typing.
Focused regressions cover automatic provider setup and restoration, configuration
ownership/conflicts, dependency bootstrap, WebSocket forwarding, consultant model
routes, prohibited tools and own-draft publication. Generated profiles and content
hash validation passed. `git diff --check` passed; changed documentation links were
checked locally. These checks establish source behavior, not live agent compliance.

## CLI observations

The focused ordinary interactive CLI scenario ran on Luna high. One Sol medium
native consultant completed two bounded consultations. The first requested the
missing database/cache/timing trace. The follow-up read the newly archived trace,
the original evidence and the previous conclusion, then recommended cache
consistency work with explicit executor-owned acceptance checks. The coordinator
published four newest-first pipeline editions and did not declare a fix verified.
This final scenario supplied the additional trace as user evidence; it did not
exercise fresh evidence collection by a debugger or implementation verification.

All 94 observed call records and 27 MCP outcomes were reviewed. Every server-side
MCP outcome succeeded, with no replayed writes. The consultant's observed calls
were tool discovery, four selected report reads, two draft creations, own-draft
patches, publication and native results. No consultant terminal, project, network,
catalogue, child-agent or app-message access was observed. Both draft patches
matched drafts issued to that same participant. The coordinator's command reads
loaded instructions and publication guidance.

The strict audit **failed**. Tool discovery produced a truncated result and was
repeated more narrowly. A publication wrapper had a syntax error before dispatch,
then the consultant published successfully. The second report also copied one
SHA-256 incorrectly (49 hexadecimal characters); the two source-report hashes
matched their files. Its claim that every digest matched is therefore unsupported.
The useful recommendation does not erase these failures.

The host retained an opaque native assignment and no registered role identity for
the arbitrary agent name. The observer reports `worker_assignment_policy_unverified`
and treats the unclassified Sol participant as outside the ordinary Luna route.
Manual review can establish the observed report-only behavior and actual model,
but cannot establish the hidden assignment's complete policy. These are audit
limitations, not proof of sandbox enforcement or permission to relax acceptance.

Ordinary gateway connections preserved Luna high and Sol medium. A single explicit
`/compact` completed visibly; gateway metadata recorded the compaction trigger
with Luna effort changed from high to medium. The composer remained Luna high
afterwards. The send helper reported no ordinary user-turn receipt for this slash
command; it was not retried. Compaction was established separately by the gateway
event and completed UI state. Automatic threshold triggering was not forced.
The CLI exit marker was zero, and the exact session and temporary observation
streams were stopped only after complete calls/events capture and audit.

## Desktop observations

Actual Desktop ran immediately after CLI on the same unchanged payload, with a
disposable Electron profile. The prepared composer visibly showed Luna high;
initial submission produced one task receipt. Four passive MCP initializations
matched the candidate version and seven-tool catalogue. Gateway connections
preserved Luna high and Sol medium through both consultations.

The consultant completed two advisory reports and the coordinator published four
pipeline editions. It requested missing evidence first and changed its
recommendation after reading the archived new trace and all pages of its earlier
conclusion. Every cited SHA-256 in both Desktop reports matched the referenced
file. The consultant used only discovery, selected report reads, own-draft reads
and patches, publication, and native results. The observed agent name allowed
classification as `senior_consultant`; no forbidden access was found. Both draft
patches matched that participant's observed draft receipts.

All **104 call records and 34 MCP outcomes** were reviewed, including the complete
tails before stopping Desktop. All server outcomes succeeded, no writes replayed,
and no command sessions or execution cells remained open. The strict audit still
**failed** on one truncated tool-discovery result and the hidden native assignment
policy. There were no recorded tool execution errors in this Desktop run.

The steering helper also rejected its exact-delivery check: the first ASCII `N`
arrived as Cyrillic `Т`, followed by Desktop's paragraph newline. The archived
source proved the mutation; the prompt was not resubmitted. The agent processed
the new evidence and completed, but this is not a passing literal-delivery test.
The helper's failure is retained separately from the successful MCP outcomes.
The disposable Desktop process and temporary observation streams were stopped
after review. No clean consecutive CLI/Desktop parity is claimed.

## Consultation usage

One consultation means one bounded assignment/continuation, not one model response.
The final CLI pair used two consultations and **13 Sol medium model responses**:
454,918 input tokens, including 416,896 cached input tokens, and 3,347 output
tokens (458,265 total). Non-cached input was 38,022 tokens. Coordinator plus
consultant used 2,346,316 total tokens, including cached input. The two coordinator
turns took 396.779 seconds; this excludes idle time and the separate compaction.
Token totals are host receipts, not a monetary invoice. No dollar cost is inferred.

The two Desktop consultations used **19 Sol medium model responses**: 984,998
input tokens, including 914,304 cached input tokens, and 4,979 output tokens
(989,977 total). Non-cached input was 70,694 tokens. Desktop coordinator plus
consultant used 3,111,741 total tokens; its two coordinator turns took 507.261
seconds. Across both final hosts, four consultations used 32 Sol responses,
108,716 non-cached input tokens and 8,326 output tokens. Host tool/instruction
context and repeated publication reads still contribute significant input cost.

An earlier rejected candidate completed one consultation and an ordinary writer/QA
flow, but the consultant loaded instructions through a terminal and other workers
had recovered cursor/patch errors. That consultation consumed ten Sol medium
responses: 350,291 input (304,000 cached) and 3,426 output tokens. It is not final
candidate qualification. Two earlier startup attempts also failed: an isolated
port collision was corrected in the dev launcher, and concurrent test subprocess
bytecode invalidated a dependency tree, which was recreated before the final pair.

## Limits

The server enforces task and draft ownership. Most consultant access boundaries
remain instructions plus an audit of visible invocations; the host has not revoked
those tools. Hidden assignment content cannot be certified by this observer.
Astra escalation, Sol low/high, automatic context-threshold compaction, interrupted
consultant recovery, and a clean end-to-end executor acceptance cycle on this final
candidate have not been live qualified. Unit coverage does not substitute for them.
The clean focused CLI/Desktop pair above establishes default-route parity only;
the unrun scenarios still require their own live qualification.
